# Open applications and cold-email targets

Companies worth approaching with no advertised role. `outreach` reads and updates
this file.

An open application only makes sense where there is **no live matching role**. If a
company is advertising something you could do, that is a normal application and it
goes to `tailor` instead. Sending a spontaneous application to a company that is
actively advertising a suitable role reads as not having looked.

**Statuses**: `candidate` (worth researching) -> `researched` (channel and hook
found) -> `drafted` (staged for you to send) -> `sent` (add a `tracker.md` row) ->
`replied` / `no reply` / `skip` (with a reason).

**Channel tiers** (recorded per company, because it predicts the reply rate):

1. A dedicated open-application form the company published. Strongest: they invited it.
2. A published recruitment address (`careers@`, `jobs@`, `recruitment@`).
3. A named recruiter or hiring contact the company itself published.
4. Generic `info@` / `hello@`. Weak, and say so.

**Hook tiers**: `A` is a reason specific to this one company, and it is the default.
`B` is an honest fit at the level of the sector, allowed only if `profile.md` turns
it on, and only for outreach emails. No honest hook at either tier means `skip`.

| Company | Sector | Why them (one line) | Hook tier | Live role check (and date) | Channel | Channel tier | Status | Last contact | Notes |
|---|---|---|---|---|---|---|---|---|---|

**What this list is for, stated honestly.** When live-role checks keep coming back
"nobody here is hiring for what you do", outreach is not a way to find hidden
vacancies, because there mostly are none. It is a way to be known before the next
hiring cycle starts. Judge the sends on that basis, and do not read a low reply rate
as a verdict on you.

**When the list is worked to the bottom, source; do not redraft.** What remains on a
worked list is there because it is weak, not because it is next.

## Form-channel queue

<!-- Companies whose only channel is a form. These are apply-assist jobs, not email
     drafts. Record which KIND of form: an ordinary application form can be staged
     end to end; a talent-pool "Connect" signup creates a profile behind a consent
     tick, so most of it is yours to do by hand. Expect the best target in a batch to
     be in this table rather than reachable by email. -->

| Company | Form URL | Kind (application / talent-pool signup) | Text ready in | Status |
|---|---|---|---|---|

## Sourcing directories

<!-- Where new names come from. Industry association member lists, chamber of
     commerce directories, accelerator and VC portfolios, conference sponsor lists,
     the customer pages of tools you know well.

     Record whether each one fetches cleanly or needs a browser, what it covers, the
     URL parameters you have VERIFIED on the live page (tick one option, read the URL
     back), the directory's own "updated on" stamp, and which slices (regions,
     sectors) have been run. One slice is not the directory: one region's slice can
     yield nothing while another's yields the best hook on the list.

     Being in a directory is not a qualification: everything sourced this way still
     goes through the full pipeline. -->

| Directory | Covers | Fetches or browser? | URL and verified parameters | Slices run | Last worked |
|---|---|---|---|---|---|

## Seen and not pursued

<!-- Names examined during sourcing and left alone, with the reason, so the next run
     does not re-examine them. Group them: "large consultancies: years bar",
     "global vendors: portal-only, no local hiring decision", and so on. Keep a short
     list of the genuinely plausible ones for a run that needs names. -->

| Company or group | Reason | Date |
|---|---|---|

## Do not approach

<!-- With the reason and the date. A company that rejected you and did NOT invite a
     later approach; a company where a live application is already in flight; a
     company you have decided against. -->

| Company | Reason | Since | Revisit after |
|---|---|---|---|
